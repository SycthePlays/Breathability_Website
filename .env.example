# Supabase — Project Settings > API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Groq — console.groq.com
GROQ_API_KEY=

# Open-Meteo needs no API key (free, non-commercial tier, 10,000 calls/day).
# Routing uses the public OSRM demo server by default — no key needed,
# but it's rate-limited and meant for light/demo use. See docs/PROJECT_PLAN.md.
